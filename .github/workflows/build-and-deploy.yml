name: Build and Deploy Podman Image

on:
  push:
    branches:
      - main  # main 브랜치에 푸시될 때 실행

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        run: |
          echo "${{ secrets.GHCR_PAT }}" | podman login ghcr.io -u $GITHUB_ACTOR --password-stdin

      - name: Build Podman Image
        run: |
          podman build -t ghcr.io/${{ github.repository_owner }}/myimage:latest -f Dockerfile .

      - name: Push Image to GitHub Container Registry
        run: |
          podman push ghcr.io/${{ github.repository_owner }}/myimage:latest
